 <div class="container">
<h1>イベント一覧</h1>

  <% @events.each do |event| %>
    <div class="card mb-4">
      <h5 class="card-header"><%= link_to event.name, event_path(event.id) %></h5>
      <div class="card-text">
        <p class="mt-3"><%= l(event.start_at, format: :long) %> ~ <%= l(event.end_at, format: :long) %></p>
      </div>
    </div>
  <% end %>
</div>
